# Welcome to Test Vault B

This is the **target** vault (will receive restored configuration).

## Current Configuration (before restore)
- Theme: Default (no custom theme)
- Font: System default
- Font Size: 14px
- Has only 3 community plugins
- No custom hotkeys

## Testing Instructions
1. Open Vault A, run "Create Backup" command
2. Open Vault B, run "Restore Latest" command
3. Compare settings to verify they match Vault A's configuration
